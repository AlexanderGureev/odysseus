/**
 * ScreenshotData
 * @typedef {object} ScreenshotData
 * @property {string} endpoint - endpoint
 * @property {string} image - image
 */

/**
 * StreamData
 * @typedef {object} StreamData
 * @property {string} url
 * @property {string} protocol
 */

/**
 * Mediafile
 * @typedef {object} Mediafile
 * @property {string} track_id - track_id
 * @property {array<StreamData>} streams
 */
